import { PureComponent } from 'react';
import type { CSSProperties, TouchEvent, WheelEvent, PropsWithChildren } from 'react';
declare type ScrollAreaProps = PropsWithChildren<{
    onChange: (delta: number) => void;
    style?: CSSProperties;
    className?: string;
}>;
declare class ScrollArea extends PureComponent<ScrollAreaProps> {
    static defaultProps: {
        style: {};
        className: null;
    };
    deltaY: number;
    clientY: number;
    scrollLineHeight: number | null;
    onWheel: ({ deltaY, deltaMode, }: WheelEvent) => void;
    onTouchStart: ({ targetTouches, }: TouchEvent) => void;
    onTouchMove: ({ targetTouches, }: TouchEvent) => void;
    onTouchEnd: () => void;
    handleScroll: () => void;
    render(): JSX.Element;
}
export default ScrollArea;
