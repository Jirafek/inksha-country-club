import { PureComponent } from 'react';
import type { ReactNode } from 'react';
import type { CheckEqualType, LocaleType } from './types';
declare type YearsProps = {
    selected?: Date | null;
    onScroll: (nextValue: Date | null) => void;
    isDisabled: (value: Date, checkEqual: CheckEqualType) => boolean;
    renderLabel?: (value: Date, locale: LocaleType) => ReactNode;
    locale: LocaleType;
};
declare class Years extends PureComponent<YearsProps> {
    static defaultProps: {
        renderLabel: (value: Date) => string;
    };
    handleChange: (deltaY: number) => void;
    render(): JSX.Element;
}
export default Years;
