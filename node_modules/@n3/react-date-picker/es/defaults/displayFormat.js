import format from 'date-fns/format';
import locales from "../locales";

var displayFormat = function displayFormat(date) {
  var locale = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : 'ru';

  if (date) {
    return format(date, locales[locale].displayFormat);
  }

  return '';
};

export default displayFormat;