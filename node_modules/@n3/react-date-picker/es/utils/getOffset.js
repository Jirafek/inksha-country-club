import startOfMonth from 'date-fns/startOfMonth';
import dateToPx from "./dateToPx";
import getScrollSpeed from "./getScrollSpeed";

var getOffset = function getOffset(date) {
  var monthStart = startOfMonth(date);
  var speed = getScrollSpeed(date);
  return dateToPx(0, date, speed, monthStart);
};

export default getOffset;