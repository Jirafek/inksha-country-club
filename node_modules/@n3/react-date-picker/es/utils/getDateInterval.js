import addYears from 'date-fns/addYears';

var getDateInterval = function getDateInterval(start, length) {
  return Array.from({
    length: length
  }, function (_, i) {
    return addYears(start, i);
  });
};

export default getDateInterval;