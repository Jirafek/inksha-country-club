import type { InputHTMLAttributes, ReactElement } from 'react';
declare type InputProps = InputHTMLAttributes<HTMLInputElement> & {
    name: string;
    hasFocus: boolean | ((name: string) => boolean);
};
declare const Input: {
    ({ name, hasFocus, ...props }: InputProps): ReactElement;
    defaultProps: {
        autoComplete: string;
    };
};
export default Input;
